import { createAction } from '@ngrx/store';

export const redirectToHome = createAction(
  '[ZM-BH Portfolio] Redirect To Home Success'
);

export const redirectToUpdates = createAction(
  '[ZM-BH Portfolio] Redirect To Updates Success'
);

export const redirectToLogin = createAction(
  '[ZM-BH Portfolio] Redirect To Login Success'
);
